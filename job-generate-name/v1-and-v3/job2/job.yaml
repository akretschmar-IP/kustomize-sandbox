apiVersion: batch/v1
kind: Job
metadata:
  name: job2-
spec:
  template:
    spec:
      containers:
      - name: main
        image: alpine
        command: ["echo",  "job2"]
      restartPolicy: Never
  backoffLimit: 4
